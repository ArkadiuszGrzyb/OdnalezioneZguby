LOCAL DEVELOPMENT

To star application first we need to start postgresql database docker container
provided by us

Go to docker-compose.yml file location in resources folder and run command:
docker compose up -d

Then you can start app via OdnalezioneZgubyApplication class
